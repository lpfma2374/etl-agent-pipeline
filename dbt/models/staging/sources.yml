version: 2

sources:
  # Ajustar os nomes das tabelas raw em cada migração
  - name: raw
    schema: raw
    tables:
      - name: raw_contacts
      - name: raw_references
      - name: raw_cmdb
      - name: raw_processos_resumo
      - name: raw_indicadores_eficacia
      - name: raw_fases
      - name: raw_subseccoes_fases
      - name: raw_historico_versoes
      - name: raw_quadro_registos
