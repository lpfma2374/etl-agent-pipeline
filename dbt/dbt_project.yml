name: etl_agent
version: "1.0.0"
config-version: 2
profile: etl_agent

model-paths: ["models"]
target-path: "../target"
clean-targets: ["../target"]

models:
  etl_agent:
    staging:
      +materialized: view
    marts:
      +materialized: table
